# 🍪 @OptionalUser() Декоратор - Руководство по использованию

## Обзор

Декоратор `@OptionalUser()` предназначен для извлечения авторизованного пользователя из cookie без обязательной проверки авторизации. Это идеальное решение для эндпоинтов, которые должны работать как для авторизованных, так и для неавторизованных пользователей.

## 🚀 Основные возможности

- **Извлечение из cookie**: Автоматически извлекает JWT токен из cookie `access_token`
- **Поддержка Authorization header**: Также поддерживает Bearer токены в заголовке
- **Безопасность**: Не блокирует запросы при отсутствии или невалидности токена
- **Типизация**: Полная поддержка TypeScript с типом `User | null`

## 📝 Использование

### Базовое использование

```typescript
import { OptionalUser } from '../../common/decorators/optional-user.decorator';
import { User } from '../user/entities/user.entity';

@Get()
async getData(@OptionalUser() user: User | null) {
  if (user) {
    // Пользователь авторизован
    console.log('Пользователь:', user.username);
    return this.service.getDataForUser(user.id);
  } else {
    // Пользователь не авторизован
    return this.service.getDataForGuest();
  }
}
```

### В контроллерах

```typescript
@Controller('anime')
@UseGuards(OptionalJwtGuard) // Применяем опциональную аутентификацию
export class AnimeController {
  @Get()
  async findAll(
    @Query() query: GetAnimeListDto,
    @OptionalUser() user: User | null,
  ) {
    // user?.id будет содержать ID пользователя, если он авторизован
    return await this.animeService.findAll(query, user?.id);
  }

  @Get(':id')
  async findOne(
    @Param() params: UuidParamDto,
    @OptionalUser() user: User | null,
  ) {
    return await this.animeService.findOne(params.id, user?.id);
  }
}
```

## 🔧 Как это работает

### 1. Извлечение токена

Декоратор проверяет два источника токена в следующем порядке:

1. **Authorization header**: `Authorization: Bearer <token>`
2. **Cookie**: `access_token=<token>`

### 2. Валидация токена

```typescript
// Валидируем JWT токен
const payload: JwtPayloadDto = jwtService.verify(token, {
  secret: configService.get('JWT_SECRET'),
});
```

### 3. Получение пользователя

```typescript
// Получаем пользователя из базы данных
const user = await userService.validateUser(payload);
```

### 4. Возврат результата

- **Если токен валидный и пользователь найден**: возвращает объект `User`
- **Если токен отсутствует/невалидный/пользователь не найден**: возвращает `null`

## 🛡️ Безопасность

### Обработка ошибок

Декоратор **не выбрасывает исключения** при:

- Отсутствии токена
- Невалидном токене
- Истекшем токене
- Несуществующем пользователе

Вместо этого он просто возвращает `null`, позволяя эндпоинту работать для неавторизованных пользователей.

### Логирование

Все ошибки логируются, но не прерывают выполнение запроса:

```typescript
try {
  // Валидация токена и получение пользователя
} catch (error) {
  // Логируем ошибку, но возвращаем null
  return null;
}
```

## 📊 Примеры использования

### Список аниме с персонализацией

```typescript
@Get()
async findAll(
  @Query() query: GetAnimeListDto,
  @OptionalUser() user: User | null,
) {
  const result = await this.animeService.findAll(query, user?.id);

  // Для авторизованных пользователей добавляем дополнительную информацию
  if (user) {
    result.userInfo = {
      hasActiveSubscription: user.hasActiveSubscription,
      shouldHideAds: user.shouldHideAds,
    };
  }

  return result;
}
```

### Детальная информация об аниме

```typescript
@Get(':id')
async findOne(
  @Param() params: UuidParamDto,
  @OptionalUser() user: User | null,
) {
  const anime = await this.animeService.findOne(params.id, user?.id);

  // Добавляем информацию о взаимодействии пользователя с аниме
  if (user && anime.userAnime) {
    anime.userInteraction = {
      isFavorite: anime.userAnime.is_favorite,
      isWatching: anime.userAnime.is_watching,
      rating: anime.userAnime.rating,
    };
  }

  return anime;
}
```

### Статистика с учетом пользователя

```typescript
@Get('stats')
async getStats(@OptionalUser() user: User | null) {
  const stats = await this.animeService.getStats();

  // Для авторизованных пользователей показываем персональную статистику
  if (user) {
    stats.personal = await this.animeService.getPersonalStats(user.id);
  }

  return stats;
}
```

## 🔄 Миграция с @Request()

### Было (старый способ)

```typescript
@Get()
async findAll(@Query() query: GetAnimeListDto, @Request() req: any) {
  return await this.animeService.findAll(query, req.user?.id);
}
```

### Стало (новый способ)

```typescript
@Get()
async findAll(
  @Query() query: GetAnimeListDto,
  @OptionalUser() user: User | null,
) {
  return await this.animeService.findAll(query, user?.id);
}
```

## ✅ Преимущества

1. **Типизация**: Полная поддержка TypeScript
2. **Читаемость**: Код становится более понятным
3. **Безопасность**: Нет риска получить `undefined` вместо `null`
4. **Производительность**: Прямое извлечение без промежуточных объектов
5. **Консистентность**: Единообразный подход во всех контроллерах

## 🚨 Важные замечания

1. **Всегда используйте типизацию**: `@OptionalUser() user: User | null`
2. **Проверяйте на null**: `if (user) { ... }`
3. **Используйте OptionalJwtGuard**: Применяйте к контроллеру для корректной работы
4. **Не забывайте про user?.id**: Безопасное извлечение ID пользователя

## 🔗 Связанные файлы

- `src/common/decorators/optional-user.decorator.ts` - Реализация декоратора
- `src/common/services/optional-user-extraction.service.ts` - Сервис для извлечения пользователя
- `src/common/guards/optional-jwt.guard.ts` - Опциональный JWT Guard
- `src/modules/anime/anime.controller.ts` - Примеры использования
